# ==============================================================================
#  框选解释 · 版本库忽略规则
#
#  两条原则：
#    1. **凭据和签名材料绝不入库**（第 3 节）——
#       进了库就等于把 API Key 和 App 的签名权交出去。
#    2. 构建产物、IDE 状态、本机路径不入库 ——
#       它们每台机器都不一样，提交上去只会让每次 diff 里混进一堆噪音。
# ==============================================================================


# ------------------------------------------------------------------------------
# 1. 构建产物
#
# 各模块自己的 build/ 由模块内的 .gitignore 负责（见 app/.gitignore）。
# 这里那一行管的是根目录 —— 它放着构建报告和几个脚本的中间产物。
# ------------------------------------------------------------------------------
/build
/captures

# Gradle 与 Kotlin 的缓存。会随构建自动重建，体积能到几十 MB
.gradle/
.kotlin/

# NDK / CMake
.externalNativeBuild/
.cxx/

# 万一从别处拷进来的成品包和崩溃转储
*.apk
*.aab
*.hprof
*.iml


# ------------------------------------------------------------------------------
# 2. IDE 与本机配置
#
# .idea/ 里唯一有共享价值的是代码风格那几份，其余（SDK / JDK 路径、设备名、
# 运行配置、窗口布局）全是本机状态，换台机器就对不上。
# 想让运行配置也进库的话，把下面第二条否定规则去掉即可。
# ------------------------------------------------------------------------------
/.idea/*
!/.idea/codeStyles/

# Android Studio 生成，内容是本机 SDK 的绝对路径
local.properties

# 资源管理器的垃圾
.DS_Store
Thumbs.db


# ------------------------------------------------------------------------------
# 3. 凭据与签名材料
#
# 这三个文件不但不该入库，也不该只留在这里：
# release.jks + keystore.properties 请连同密码一起备份到项目之外
# （密码管理器 / 离线介质）。丢了就再也出不了能覆盖安装的包。
# 具体说明见 keystore.properties 和 ai.properties 各自的开头。
# ------------------------------------------------------------------------------

# release 签名材料 —— 等于 App 的私钥
/release.jks
/keystore.properties

# DeepSeek 参数（地址、模型名、超时这类非机密项）。
# 想按自己的习惯调参就把它复制出来改：cp ai.properties.example ai.properties
# 缺了这个文件构建不会失败，全部参数回落到内置默认值。
/ai.properties
